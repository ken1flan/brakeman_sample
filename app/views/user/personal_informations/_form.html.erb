<%= form_with(model: user_personal_information, local: true) do |form| %>
  <% if user_personal_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_personal_information.errors.count, "error") %> prohibited this user_personal_information from being saved:</h2>

      <ul>
      <% user_personal_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :real_name %>
    <%= form.text_field :real_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
